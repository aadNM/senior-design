The frontend needs to build separately and is not checked into git. If you are running
locally using a non packaged build, the frontend will be build on server startup
and served from a temporary directory.

This file serves as a placeholder to make sure the directory is always present when
the python package is looking for it.
